#ifndef MapContainer_class
#define MapContainer_class

#include <fstream>
#include <iostream>
#include <map>
#include <Eigen/Dense>

using namespace std;
using namespace Eigen;
class MapContainer{
    public:
    MapContainer();
    void ReadMaps();
    private:
    //variables
    public:
    private:
};

#endif